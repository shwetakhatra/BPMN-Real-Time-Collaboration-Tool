services:
  - type: web
    name: bpmn-collaboration-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:asgi_app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8000

